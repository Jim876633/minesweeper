/**
 * @description: game level enum
 */
export enum GAMELEVEL {
  EASY = "easy",
  MEDIUM = "medium",
  HARD = "hard",
}

/**
 * @description: game state enum
 */
export enum GAMESTATE {
  WIN = "WIN",
  LOSE = "LOSE",
}
